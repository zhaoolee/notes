# 一级标题 Heading 1

这是一段普通正文，用来测试**粗体**、*斜体*、***粗斜体***、~~删除线~~、`行内代码`、以及一个自动链接 <https://example.com>。

## 二级标题 Heading 2

这是另一段正文，其中包含一个[行内链接](https://example.com) 和一个脚注标记[^1]。  
这一行末尾有两个空格来测试换行。  
这是换行后的内容。

### 三级标题 Heading 3

> 这是一级引用。
>
> > 这是二级引用。
> >
> > 包含 **粗体** 和 `code`。
>
> 引用结束。

---

## 列表测试

### 无序列表

- 第一项
- 第二项
  - 二级子项 A
  - 二级子项 B
    - 三级子项 B-1
- 第三项

### 有序列表

1. 第一项
2. 第二项
   1. 子项 2.1
   2. 子项 2.2
3. 第三项

### 任务列表

- [x] 已完成任务
- [ ] 未完成任务
- [ ] 另一个待办项

---

## 代码测试

### 行内代码

请执行 `npm install` 然后运行 `npm run dev`。

### 代码块：JavaScript

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("Markdown");
```

### 代码块：Python

```python
def fib(n: int) -> list[int]:
    a, b = 0, 1
    result = []
    while a < n:
        result.append(a)
        a, b = b, a + b
    return result

print(fib(50))
```

### 代码块：JSON

```json
{
  "name": "markdown-test",
  "version": "1.0.0",
  "features": ["headings", "lists", "code", "tables"]
}
```

---

## 表格测试

| 名称 | 类型 | 状态 | 备注 |
|------|------|------|------|
| 标题 | block | ✅ | 支持多级 |
| 列表 | block | ✅ | 支持嵌套 |
| 代码 | block | ✅ | 支持高亮 |
| 表格 | block | ✅ | 对齐可能不同 |

### 对齐测试

| 左对齐 | 居中对齐 | 右对齐 |
|:------|:--------:|------:|
| left  | center   | right |
| aaa   |   bbb    |   ccc |

---

![](https://raw.githubusercontent.com/zhaoolee/ChromeAppHeroes/master/README/17106763830728faCQ0DR.png)

---

## 分隔与数学符号测试

上面有一条分割线，下面测试一些特殊字符：

- HTML 实体：&copy; &lt;div&gt; &amp; &nbsp;
- 数学/符号：± × ÷ ≠ ≤ ≥ → ← ↑ ↓
- Emoji：😀 🚀 ✅ 🔥

---

## 混合排版测试

1. 列表里放引用：
   > 这里是列表中的引用文本。

2. 列表里放代码：

   ```bash
   git status
   git add .
   git commit -m "test markdown"
   ```

3. 列表里放表格：

   | Key | Value |
   |-----|-------|
   | A   | 123   |
   | B   | 456   |

---

## HTML 兼容测试

<b>这是 HTML 粗体</b><br>
<i>这是 HTML 斜体</i><br>
<kbd>Ctrl</kbd> + <kbd>C</kbd>

---

## 脚注测试

这里有一个脚注引用[^longnote]。

[^1]: 这是一个简单脚注。  
[^longnote]: 这是一个较长的脚注内容，用来测试脚注区域的渲染效果。

---

## 最后一段

> **Markdown 渲染测试完成。**  
> 你可以重点观察：标题大小、段落间距、列表缩进、代码高亮、表格边框、引用样式、图片显示、脚注位置，以及 HTML 是否被正确处理。
