services:
  app:
    image: ${NOTES_EXPORTER_IMAGE}
    ports:
      - "${NOTES_EXPORTER_PORT:-18080}:3001"
    environment:
      PORT: "3001"
      IMAGE_STORAGE_DIR: /app/storage/images
    volumes:
      - ./storage/images:/app/storage/images
