# 项目长期事实

本文件只记录已从代码或 feedback 测试中确认、预计会影响后续任务的信息。临时调试输出和未经验证的推测不写入这里。

## 2026-07-31：移动分享面板对齐原版操作层

- 手机预览态的分享面板采用原版“请选择操作”底部操作层：全宽贴底，只保留顶部
  `16px` 圆角，`52px` 标题栏居中，右侧关闭键无边框。
- 动作区使用浅灰底上的纯白卡片，左右留白 `16px`、间距 `18px`，卡片至少
  `58px` 高且不显示技术说明；真实动作顺序为复制文字、复制到公众号、以图片形式
  分享、导出离线归档。
- 遮罩加深为 `rgba(20, 18, 17, .58)`；面板与遮罩有短进入动画并兼容
  `prefers-reduced-motion`。暗黑主题复用相同几何，标题栏、关闭键和动作卡片均
  使用暗黑色板。
- `390 × 844` 浏览器视口实测面板为 `390 × 387px`、左右及底部均为 `0`，
  标题栏 `53px`（含分隔线），四张动作卡片均为 `58px`，页面横向溢出为 `0`；
  关闭按钮可正常移除对话框。

## 2026-07-26：Harness 目录落地

- 智能体入口为根目录 `AGENTS.md`。
- 项目上下文位于 `DOCS`。
- 仓库工具说明位于 `TOOLS`。
- 固定流程位于 `PROMPTS`。
- 前端 feedback 测试位于 `frontend/tests`。
- 后端 feedback 测试位于 `backend/tests`。
- 统一 feedback 命令为 `npm test`。

## 架构事实

- 前端源码当前位于 `src`，后端源码当前位于 `server`。
- `NoteSheet` 和 Markdown 分段逻辑由前端预览、Playwright PNG 导出和离线归档共同复用。
- PNG 导出依赖 Playwright Chromium 打开可访问的前端页面。
- 单容器镜像由 Express 提供前端静态文件；双容器生产环境由 Nginx 代理 API 和图片。
- `storage/images` 是导入图片与已生成 PNG 的持久化边界。
- 账号、按用户隔离的云端工作区和匿名额度使用
  `storage/data/notes-data.json` 持久化；会话使用服务端签名 Cookie。

## 2026-07-26：Markdown 视觉兼容性测试

- 可重复使用的测试数据位于 `frontend/tests/markdown-support-visual.md`。
- 自动语义测试位于 `frontend/tests/markdown-support.test.ts`。
- 截图和逐项评判位于 `frontend/tests/markdown-support-report.md`。
- 已确认支持常用标题、强调、删除线、链接、引用、列表、代码块、GFM 表格、图片、脚注和复合嵌套。
- `##` 是项目的便签区段分隔符，不按标准 H2 内容渲染。
- 单个软换行会被项目插件拆成手动换行段落，和 CommonMark 默认行为不同。
- 原始 HTML 不会生成 HTML 元素，而是按字面文本显示。
- 任务列表同时显示列表圆点和复选框。
- 默认主题下链接对纸张背景的对比度约为 2.93:1，引用文字约为 1.97:1。
- 脚注功能可用，但 `Footnotes` 辅助标题因缺少隐藏样式而可见。

## 2026-07-26：全局操作收纳

- 此处最初将主题、新建、插图、示例、删除、归档和复制统一放入设置面板；该方案已被后续“移动端设置与详情操作原生化”取代。
- 当前桌面顶栏保留存图、分享与设置；手机端按列表 / 编辑 / 预览状态切换对应操作。
- 便签底部品牌文案既可在预览中就地编辑，也可从全局设置的“便签底部文字”
  二级页修改；两处共享同一状态，并用于预览、导出、归档和公众号富文本。
- `EditorPanel` 通过 `EditorPanelHandle.openImagePicker()` 向详情顶栏暴露图片选择能力，粘贴和拖放图片流程保持不变。

## 2026-07-26：本地多便签工作区

- 前端状态包含便签集合和当前便签 ID，每张便签独立保存 Markdown、创建时间和更新时间。
- 工作区使用 `notes.workspace.v1` 持久化到浏览器 `localStorage`；旧的 `notes.markdownDraft` 会在首次加载时迁移为第一张便签。
- 大于 `640px` 时使用常驻左侧栏，提供搜索、新建、切换和带确认的删除；项目不再保留中间宽度的平板抽屉布局。
- 侧栏只显示更新时间和从 Markdown 自动提取的标题或第一句话；列表采用 50px 行高、20px 左留白、12px 时间和 14px 标题。选中项仅使用浅背景与单层内阴影，不显示左侧色条或额外顶线。
- 删除最后一张便签时会自动创建新的空白便签，工作区始终至少保留一张。
- 存图、归档、复制和插图都只作用于当前选中的便签；示例内容只用于首次初始化，不再作为正式界面操作。
- 匿名多便签数据只存于浏览器本地；普通用户或超级管理员登录后改用按账号 ID
  隔离的服务端工作区。
- 前端 feedback 覆盖工作区数据校验、标题摘要提取和侧栏结构；浏览器验收确认新建、切换、刷新持久化、搜索与删除流程可用。

## 2026-07-26：notes-export-api Skill 端到端验证

- 已安装的 `notes-export-api` Skill 可调用当前服务完成默认主题、`smartisan-dark` 主题和本地图片 Markdown 导出。
- 本地图片会先上传到同源 `/api/images/import`；包含 3 张本地图片的测试文档成功导出为 `1980 × 33660` PNG，长图分片与拼接链路可用。
- 多便签前端改造没有污染 Playwright 导出画面；`renderMode=playwright` 仍只呈现便签纸张。
- 仓库内 Skill 支持 `--footer-brand` 和 `--footer-via`，已安装版本尚未包含这两个参数，存在版本漂移。
- Skill 脚本当前使用 `curl -sS` 且不校验响应状态或 PNG 类型；后端返回 500 JSON 时仍可能以 `.png` 保存并以成功状态退出。
- 后端会缓存 Playwright 浏览器启动 Promise；首次启动失败后，即使补装 Chromium，同一后端进程仍需重启才能恢复导出。

## 2026-07-26：锤子便签网页版视觉基准

- 在 `yun.smartisan.com` 当前网页版中，`1622px` 视口下的桌面布局实测为 `255px` 文件夹栏、`419px` 便签列表和 `948px` 编辑区，对应约 `15.72% / 25.83% / 58.45%`；三者相邻且没有木纹间隙。
- 网页版顶栏总高 `62px`，渐变从 `#827265` 到 `#716053`，底线为 `1px #564944`，阴影约为 `0 1px 8px rgba(0,0,0,.15)`。
- 网页版普通便签项为 `50px` 高、背景 `#fbf7ed`、底部分隔线 `1px #e4dad1`；选中项背景 `#f1ece1`，仅增加 `inset 0 1px 4px rgba(0,0,0,.03)`，没有左侧色条或额外顶线。
- 网页版便签项左内边距为 `20px`；时间是 `12px/20px`、颜色 `#b9a691`，标题是 `14px/20px`、字重 `400`、颜色 `#635753`。
- 网页版搜索框宽度为所在栏宽减 `20px`，高度 `29px`、圆角 `14px`、背景 `#fcfcfc`、边框 `1px #e4dad1`，内阴影为 `0 1px 4px rgba(0,0,0,.12)`。
- 网页版编辑纸横线每 `50px` 一条、线宽 `1px`；本项目已经对齐为每 `50px` 一条、线宽 `1px`、颜色 `#e8e2d7`。
- 网页版界面字体优先使用 `PingFang SC`；正文编辑字体为 `17px/50px`、字重 `400`，编辑内容距纸面左侧 `50px`。

## 2026-07-27：桌面三栏视觉对齐网页版

- 桌面三栏改为 `clamp(180px, 15.72vw, 360px) / clamp(280px, 25.83vw, 590px) / 剩余空间`；在 `1622px` 视口下与网页版实测 `255 / 419 / 948px` 一致。栏间使用 `1px #dcd6ca` 分隔，编辑区右侧保留网页版同量级的 `1px 0 3px rgba(0,0,0,.1)` 轻阴影。
- 桌面编辑器取消 28px 外边距、玻璃背景、边框和卡片阴影，直接铺满横线纸；正文使用 `PingFang SC` 优先、`17px/50px`、字重 400，并以 `30px` 装订边加 `20px` 正文留白形成 `50px` 左缩进。
- 桌面预览区不再透出木纹，改为每 `50px` 一条细线的暖白纸面，并增加 `50px` 的浅色状态栏和 `1px #e3dcd1` 分隔线；成品便签仍保留，PNG 导出模式和手机预览不受该桌面样式影响。
- 桌面搜索框改为所在栏宽减 `20px`；界面与 Markdown 编辑器统一为 `PingFang SC` 优先，成品便签继续使用原有 `OPPOSans`，避免改变 PNG 输出。桌面顶栏左侧标识改为网页版的三横线视觉，项目标题和现有导出、分享、设置功能保持不变。

## 2026-07-27：桌面编辑与预览互斥切换

- PC 正文工作区从“Markdown 编辑 / 实时预览”并排改为单一区域互斥切换；完整页面仍保留“分类 / 便签列表 / 单一正文工作区”三栏。实时预览默认关闭，初始视图固定为 Markdown 编辑。
- 正文区顶部保留与锤子便签网页版一致的 `50px` 状态栏，左侧 `125 × 25px` 模式下拉框可在“Markdown 模式 / 实时预览”之间切换，右侧显示全部便签、更新时间和字数。
- 新建或选择便签时，PC 正文区自动回到 Markdown 编辑，避免用户在预览态进入新便签后误以为无法编辑。
- 编辑器和成品预览占用同一网格区域，任一时刻只显示一个；手机端仍使用原有 `notes / editor / preview` 三态工作区。
- Playwright 导出模式继续同时挂载编辑器和预览组件，隐藏桌面状态栏，因此填写 Markdown、等待渲染和截取 `.note-sheet` 的链路不受影响。

## 2026-07-27：官方分类、加星与回收站

- `NoteDocument` 已增加 `folderId`、`isStarred` 和 `deletedAt`；工作区增加自定义 `folders`。仍沿用 `notes.workspace.v1`，旧数据加载时会安全补齐默认值。
- 桌面端按官方比例呈现 `15.72%` 分类栏、`25.83%` 便签列表和剩余正文区；分类栏包含“全部便签”“加星便签”“回收站”和自定义文件夹，并显示实时数量。
- 此处最初采用全屏分类面板，已被后续“安卓原版分类锚定浮窗”替代；仍不增加 iPad 或中间宽度布局。
- 加星便签会进入“加星便签”过滤视图；全部、加星和文件夹分类都自动排除已删除便签。
- 普通删除改为软删除到回收站；回收站提供恢复和带二次确认的永久删除。删除最后一张正常便签时仍创建空白便签，但被删除内容继续保留在回收站。
- 自定义文件夹支持创建、选择和删除；删除文件夹只将其中便签改为未分类，不会删除内容。桌面与手机详情元数据栏都可修改当前便签的文件夹归属。
- 搜索关键字由分类栏和便签列表共享；桌面搜索位于分类栏，手机搜索仍位于便签列表。
- feedback 新增旧数据迁移、分类过滤、加星、文件夹归属、回收站、恢复与分类侧栏结构测试。浏览器验收确认创建/删除文件夹、加星过滤、软删除与恢复数量闭环可用；390 × 844 手机视口无横向溢出。

## 2026-07-27：桌面与手机统一对齐网页版资源（手机部分已废弃）

- 已从锤子便签网页版实际加载的静态域名保存 23 个技术验证所需 UI 图片到 `public/smartisan/web`，来源、日期和权利边界记录在该目录的 `README.md`；`TOOLS/download-smartisan-web-assets.sh` 可重复抓取。
- 当时曾让桌面和手机默认主题统一使用网页版 Sprite 与 `62px / 50px` 节奏；2026-07-28 用户用已连接安卓客户端明确否定该方向，手机部分不再生效。
- 当时曾把手机列表从 APK 75.3333px 九宫格纸片改为网页版 50px 平面列表；该方案已废弃，手机恢复以 APK 为视觉和交互基准。
- 官方网页自身设置 `min-width: 1280px`，因此手机不能字面照搬其桌面宽度；项目使用同一套视觉 Token 和图片，同时保留 `notes / editor / preview` 分层交互。
- 修复了桌面默认编辑视图选择器覆盖手机预览的问题：手机进入 `preview` 后 `.preview-panel` 现在以同等或更高优先级显示，浏览器截图确认成品便签不再是空木纹。

## 2026-07-26：侧栏视觉对齐落地

- 默认主题侧栏已经使用网页版基准色：普通项 `#fbf7ed`、选中项 `#f1ece1`、分隔线 `#e4dad1`、时间 `#b9a691`、标题 `#635753`。
- 侧栏改为纯纸面背景，只保留右侧 1px 分隔线；取消玻璃模糊、整块阴影和桌面三栏之间的 18px 木纹间隙。
- 侧栏标题区从 78px 压缩为 50px，并隐藏 `NOTES` 眉题；搜索框改为 `248 × 29px`、14px 圆角和网页版同款内阴影。
- 便签项固定为 50px；时间使用 `12px/20px`，标题使用 `14px/20px`、字重 400，并在侧栏内优先使用 `PingFang SC`。
- 顶栏总高对齐为 62px，采用 `#827265 → #716053` 渐变、`1px #564944` 底线和 `0 1px 8px rgba(0,0,0,.15)` 阴影。
- 编辑纸横线间距从 49px 调整为 50px；新增 feedback 测试锁定侧栏核心视觉参数。

## 2026-07-26：安卓版基准与移动端三态工作区

- 在逻辑尺寸 `360 × 800dp` 的安卓真机上，通过 ADB 截图与 UI 层级确认：安卓版锤子便签使用列表页和独立编辑页，不会把导航、编辑器与预览纵向堆叠。
- 安卓客户端顶栏约 `48dp` 高；列表搜索框左右约 `12dp` 留白；单条便签约 `75dp` 高，以暖白纸面、细边框和轻阴影覆盖在木纹背景上。
- 本项目在不超过 `640px` 时改为 `notes / editor / preview` 三态工作区：首次进入显示列表，选择或新建后进入编辑，预览与编辑互斥显示，页内返回回到列表。
- 移动端顶栏与 APK 一致为 `48px`，顶栏下方详情元数据栏为 `50px`。
- 移动端便签条目使用 APK 转换后的 `75.3333px` 九宫格纸片，无额外条目间距；选中项只使用轻内阴影。
- 移动端列表不显示删除按钮；删除只在预览态顶栏出现，并继续使用确认对话框。
- 已在同一台安卓真机的 Chrome 中验证列表 → 编辑 → 预览 → 返回列表，以及软键盘动态视口；feedback 测试和完整生产构建均通过。

## 2026-07-26：移除平板布局

- 项目只保留桌面三栏和不超过 `640px` 的手机三态工作区，不再提供 641–1360px 的导航抽屉与编辑/预览上下堆叠布局。
- 桌面三栏最初使用 `clamp(220px, 21vw, 286px)` 侧栏和不超过视口 `50%` 的预览栏；该比例已被后续“桌面三栏视觉对齐网页版”的 `15.72% / 25.83% / 剩余空间` 基准取代。
- 常见 1280–1360px 桌面视口会继续显示三栏，不再进入类似 iPad 的堆叠界面。

## 2026-07-26：移动端返回按钮防裁切

- 移动端编辑/预览顶栏采用 `36px / minmax(0, 1fr) / auto` 三列网格，三个可见元素显式固定到对应网格列。
- 返回按钮直接使用 APK 的 `36 × 36dp` `btn_back.png`，不再使用 SVG 或 CSS 伪元素拼接箭头。
- 安卓真机确认返回按钮完整可见，便签标题不会覆盖右侧的存图和设置操作。

## 2026-07-26：安卓版 APK 资源级设计基准

- 用户设备 `Download` 中的安卓版锤子便签 APK 已完成只读拉取和资源级静态解包；包名为 `com.smartisan.notes`，版本为 `4.2.1`，versionCode 为 `42100`。
- APK SHA-256 为 `ea7249a92756d3c36592beb216d5c9bd74a3a1b0cbcd0a9f74aa738532eb104f`。
- 核心设计资源、布局和 selector 已筛选到 `DOCS/references/smartisan-notes-android-4.2.1`，仅用于内部视觉分析，不默认进入产品构建。
- APK 精确确认手机顶栏为 `48dp`、按钮画布为 `36dp`、标题为 `20sp`；列表 9-patch 纸片可见高度约 `75.3dp`。
- 列表普通态和按下态分别使用不同的 9-patch 纹理，代表性中心像素约为 `#F9F6ED` 与 `#E0DDD5`；选中/按下效果不依赖左侧色条。
- 列表标题色为 `#FF5C3C2A`、字号为 `16.5sp`，时间为 `12sp`；正文基准为 `16sp`、`42dp` 行高、左右内边距 `39dp / 18dp`。

## 2026-07-26：授权 APK 素材接入移动版

- 用户确认已取得安卓版锤子便签素材授权；核心移动素材位于 `public/smartisan/mobile`，会随前端构建发布。
- 手机顶栏使用 APK 的 `action_bar_default.png`；返回、设置、新建、插图、完成、删除和分享分别使用 `btn_back.png`、`btn_settings.png`、`btn_create.png`、`btn_pic.png`、`btn_save_notes.png`、`btn_delete_notes.png` 与 `btn_share_notes.png`。白色按钮素材只放在深棕顶栏。
- Android 9-patch 列表纸片和搜索框已去除 1px 标记并拆成左、中、右 Web 切片；浏览器固定两端、只拉伸中间区域。
- 移动列表使用原始木纹、装订夹和图片状态图标；纸片高度为 `75.3333px`，条目连续排列，没有额外底部间距。
- 持久选中项继续使用普通纸片和轻内阴影；APK 灰色纸片只用于真实按下状态。
- 默认主题声明 `color-scheme: only light`，防止安卓浏览器强制夜间模式篡改纸张素材；主动选择暗黑主题不受影响。
- 已在 `1080 × 2400`、480dpi 安卓真机浏览器中验证列表、编辑、软键盘、预览和返回流程。

## 2026-07-26：移动端按原生页面层级重排

- 安卓真机确认原生列表页的设置、“全部便签”和新建都位于同一条 `48dp` 深棕顶栏；搜索框直接位于其下，不存在额外的白色标题块或便签数量徽标。
- 项目移动列表已同步该层级：左设置、中标题、右新建；列表内的标题区在手机断点下隐藏，便签数量从组件中彻底移除。
- 原生详情页在 `48dp` 顶栏下保留约 `50dp` 的归属、时间和字数元数据栏；点击正文进入编辑并弹出键盘时，该栏仍然可见。
- 项目详情页以紧凑元数据栏取代原来的 `44px` 大分段按钮；编辑/预览使用右侧小按钮切换。编辑页为全屏横线纸，预览页复用真实 `NoteSheet` 成品便签，在木纹背景上按手机宽度缩放并独立滚动，保留边框、角标、Markdown 排版和底部署名。
- 详情顶栏不重复显示便签标题。编辑态采用“返回 / 弹性留白 / 插图与完成”，预览态采用“返回 / 弹性留白 / 删除与分享”，避免长标题挤压按钮。

## 2026-07-26：原生置顶与拖拽排序规则

- 安卓真机使用三条已置顶便签验证：置顶分组始终位于普通分组之前。
- 普通便签被置顶时会插入置顶分组最前方；最近置顶的便签排在最上方。
- 置顶分组内部不支持拖拽排序。长按第一条置顶便签并跨过另外两条释放后，没有出现浮起拖动态，三条顺序保持不变。
- 取消置顶会把便签放回普通分组中保留的普通排序位置，而不是简单追加到底部。
- 普通便签长按拖拽时会显示浮起纸片、阴影和 APK 灰色按下纹理；实现项目排序时应只允许普通分组拖拽。
- 真机测试完成后，三条测试便签的置顶状态和“运维神器爱马仕 → 班味儿 → test”顺序已恢复到测试前。

## 2026-07-26：Web 移动端置顶与拖拽实现

- `NoteDocument` 使用 `normalOrder` 保存普通分组顺序、使用可空的 `pinnedAt` 保存置顶状态和置顶先后；旧版 `notes.workspace.v1` 会按原数组顺序自动补齐这两个字段。
- 列表统一按“`pinnedAt` 倒序的置顶组 + `normalOrder` 正序的普通组”呈现。置顶只更新 `pinnedAt`，取消置顶不会丢失 `normalOrder`。
- 普通组使用 dnd-kit 的鼠标、触摸和键盘传感器排序；手机触摸需按住 `500ms`，容差为 `8px`。置顶组及搜索状态下禁用拖拽。
- 移动列表右上角直接使用授权 APK 的 `icon_top_normal.png` 与 `icon_top_checked.png`；拖拽态使用授权 APK 的灰色按下纸片切片和浮起阴影。
- 前端 feedback 已覆盖旧数据迁移、最近置顶优先、置顶区禁拖、取消置顶回位和普通区排序隔离。
- 在 `1080 × 2400`、480dpi 安卓真机 Chrome 中验证：连续置顶顺序正确，置顶长按不移动，普通便签按住后可浮起换位，取消置顶回到保留的普通位置；测试后已清理临时便签并恢复原浏览器数据。
- 原版 App 真机逐帧核对确认：左侧金属装订夹是独立于纸片的固定装饰轨道，不随单张纸片拖拽位移；拖拽开始时整条轨道向左收起，只留下夹头，释放后再弹回。Web 端将装订夹移出 sortable 条目，以列表级轨道统一渲染，并由拖拽状态驱动 `translateX(-19px)`，过渡时长为 `200ms`。
- 曾尝试把固定装订夹等比缩放为 `19 × 50px` 以适配网页版手机列表；该方案已于 2026-07-28 废弃。手机轨道必须保持 APK 的 `28.6667 × 75.3333dp` 画布，拖拽收起 `19px`；桌面断点继续隐藏该轨道。
- 原版图钉位于收藏星标左侧；Web 移动端即使暂不显示收藏，也为该位置预留空间，将图钉画布从列表盒右边缘内收 `43px`，避免越过纸片的 9-patch 可见右边界。
- 拖拽纸片不能对整行 sortable 容器添加 CSS 阴影，因为该容器还包含纸片两侧的透明边距，会形成大于便签本体的矩形浮层。移动拖拽态只使用 APK 灰色 9-patch 自带的边缘和底部阴影。
- `--note-sheet-width` 不能只定义在根节点并间接引用 `--note-scale`：根节点会先用桌面比例把它解析为 `660px`，移动预览即使把舞台比例更新为约 `1.03` 仍会继承错误宽度。预览舞台必须本地重新定义 `--note-sheet-width: calc(330px * var(--note-scale))`。

## 2026-07-26：移动端设置与详情操作原生化

- 安卓原版设置是独立全屏页面，只包含跨便签长期生效的偏好；新建、插图、删除和分享分别属于列表页或详情页，不进入设置。
- Web 手机列表顶栏继续使用“设置 / 全部便签 / 新建”。齿轮只在列表页出现，打开带返回顶栏的全屏设置页。
- 当前真实长期偏好包括背景主题和便签底部文字：背景二级页选择“暖白纸感 /
  深夜便签”，底部文字二级页编辑发送来源与 `via` 文本。账号、云同步、密码锁、
  字号等未实现能力不会以假入口呈现。
- 手机编辑态顶栏为“返回 / 插图 / 完成”，完成后进入预览；预览态顶栏为“返回 / 删除 / 分享”，与原版编辑态和查看态职责一致。
- 分享使用底部面板承接 Web 特有能力：保存当前便签长图、复制 Markdown、下载离线归档。加载示例不再出现在正式界面中。
- 桌面仍保留直接“存图”，并新增独立分享按钮承接复制与归档；桌面设置与移动端
  共用背景和底部文字偏好。
- 在 `1080 × 2400`、480dpi 安卓真机 Chrome 中确认全屏设置、背景二级页、编辑 / 预览顶栏及分享底部面板均无裁切或越界。

## 2026-07-27：安卓原版分类锚定浮窗

- 在 `1080 × 2400`、480dpi 安卓真机上实测原版锤子便签：点击列表顶栏分类名称旁的小三角后，分类选择器覆盖在便签列表上方，不替换 `body`、不隐藏搜索框，也不改变列表滚动位置。
- 原版浮窗没有暗色遮罩；换算到 `360 × 800dp` 后左右各约 `31dp`、距内容区顶部约 `16dp`、圆角约 `8dp`。每个分类行约 `48dp`，底部编辑 / 排序 / 新建文件夹操作区约 `45dp`。
- Web 手机端改为同样的顶栏锚定浮窗：透明点击层只负责点外部关闭，列表主体继续显示；选择分类、再次点击标题、点击外部或按 `Escape` 都会收起。
- 浮窗复用全部便签、加星便签、回收站和自定义文件夹数据；底部增加文件夹编辑、名称正倒序和新建文件夹入口。桌面常驻分类栏不显示该浮窗底部工具条。

## 2026-07-27：桌面网页版像素基线与前 20 条测试数据

- 在用户已登录的 `https://yun.smartisan.com/#/notes` 上按 `1596 × 1195` 视口实测：分类栏约 `251px`，便签列表约 `414px`，正文工作区从 `x = 665px` 开始；顶栏和状态栏分别为 `62px / 50px`。
- 官方便签列表从顶栏下方直接开始，单条固定 `50px`，没有额外的列表分类标题行；项目桌面端已同步。
- 官方桌面顶栏为“刷新 / 新建”、“插图 / 转移”、“分享 / 删除”三组 `68 × 38px` 按钮；桌面设置齿轮和“存图”文字按钮不显示。
- 编辑模式切换改为锚定浮窗，触发器 `125 × 24px`，浮窗宽 `202px`，不再使用浏览器原生 `select`。
- 官方列表前 20 条便签已完整写入 `src/fixtures/smartisan-web-test-workspace.ts`，包含正文、顺序、时间和 4 个原始图片链接。
- `?testData=smartisan-web-20` 使用独立存储键 `notes.workspace.smartisan-web-20.v1`；`&resetTestData=1` 是一次性还原指令，初始化夹具后会从当前 URL 消费掉，后续刷新读取持久化工作区，不会丢失新建便签，也不会覆盖正常工作区数据。
- 桌面刷新按钮不能直接同步重载页面：点击后先让官方刷新 Sprite 顺时针旋转一圈
  （`600ms`），再于 `650ms` 重载；动画期间通过计时器引用拦截重复点击。

## 2026-07-27：桌面网页版第二轮交互对齐

- 官方分类栏和便签列表底部各有 `50px` 固定操作区，背景 `#fbf7ed`、上边线 `1px #e9dece`；分类栏下载文字为 `12px #9d8369`，项目使用从官方 DOM 提取的同路径 `20 × 20px` SVG。
- 官方便签栏底部按钮可收起分类栏：分类栏宽度变为 `0`，便签栏移动到 `x = 0`，正文区跟随前移；按钮 Sprite 从 `-287px -220px` 切换到 `-247px -220px`。项目已复现并通过浏览器尺寸验证。
- 官方点击分享会切换到整页分享预览：顶栏显示取消与分享操作，右侧从 `y = 62px` 开始显示木纹舞台，`660px` 便签纸居中于正文栏。项目用复制 Markdown、下载归档和保存图片映射现有真实能力。
- 项目退出分享预览时会恢复进入分享前的编辑/预览模式；分享态不会替换分类与便签列表，也不会污染移动端底部分享面板。
- 项目模式菜单只保留 Markdown / 实时预览两项，但维持官方浮窗约 `110px` 的整体高度；两项各为 `52px`，保证第二项完整的视觉与点击高度。

- 普通便签列表不再渲染悬浮删除叉号，删除入口唯一收敛到桌面右上角红色垃圾桶；回收站的恢复和永久删除按钮不受影响。
- 官方与项目的 PC 字号、字体族和 `font-weight: 400` 原本基本一致；项目看起来偏细的根因是全局 `-webkit-font-smoothing: antialiased`，而官方使用 `auto`。桌面普通工作区已恢复 `text-rendering / -webkit-font-smoothing / -moz-osx-font-smoothing: auto`，移动版和 PNG 导出保持原设置。

## 2026-07-27：方括号居中正文语法

- 锤子便签 Markdown 的首个非空行若整行符合 `[文字]`，预览和导出应去掉两侧方括号，并将内部 Markdown 作为普通正文居中渲染。
- 这项语法只改变 `text-align`；不得附加标题字号、加粗、标题颜色或额外段间距。方括号内显式书写的 Markdown（例如 `**粗体**`）仍按用户原意渲染。
- `splitSections` 只在文档顶部识别该语法；正文中的 `[文字]`、`[链接](url)` 和图片不会被误转换。
- `NoteSection.headingAlignment` 继续记录这一兼容语法；`NoteSheet` 把该行放进 `.note-copy`，仅由 `.note-centered-line` 提供居中。网页预览、移动预览、PNG 导出和归档 HTML 因复用同一组件而同步生效。

## 2026-07-27：本地开发导出服务可靠启动

- Vite 单独启动时，`/api/export` 会因为 `127.0.0.1:3001` 没有 Express
  监听而返回无正文的代理层 500；普通编辑仍然正常，因此问题容易被误判成
  Playwright 渲染失败。
- `npm run dev` 现在由 `scripts/dev.mjs` 同时启动 Vite 和 Express，并为
  后端设置开发态 `EXPORT_APP_URL`；需要拆分调试时才使用
  `npm run dev:frontend` 或 `npm run backend:watch`。
- Playwright 浏览器启动 Promise 在启动失败或浏览器断线后会被清除，下一次
  导出可以重新拉起 Chromium，不再要求重启整个 Express 进程。
- 隔离端口从零启动验收通过；仓库内 `notes-export-api` Skill 成功生成
  `1980 × 27606`、约 4.1 MB 的有效 PNG。

## 2026-07-27：一键复制微信公众号富文本

- 桌面分享预览与手机分享面板新增“复制到公众号”；操作会先准备图片，再向系统
  剪贴板同时写入 `text/html` 富文本和 `text/plain` Markdown。
- 公众号 HTML 由 `WechatArticle` 独立渲染，使用低对比暖米色纸沿、暖白纸张、
  双层细框、轻阴影、常规字重的棕色正文与底部署名还原锤子便签成品视觉；标题、段落、引用、
  列表、代码、表格、链接和图片全部使用内联样式，避免依赖公众号编辑器会过滤
  的外部 CSS。顶部整行 `[文字]` 会去除方括号，仅以普通正文样式居中。
- 公众号富文本颜色必须直接复用 `styles.css` 的成品便签 Token，而不能重新目测
  取色；正文使用 `rgba(106,86,67,.92)`、标题使用
  `rgba(70,53,38,.96)`、纸面使用 `#fefcf6`。
- 成品便签外框四角的独立小正方形属于必要视觉元素；公众号保存会移除绝对定位，
  使四个普通节点回流成左上角竖条，因此公众号 HTML 使用一个 3×3 展示表格来
  承载四个带 NBSP 的真实角单元格和四条边。以成品长图
  `example/程序员狠话Vol.5.JPG` 为准，方格位于主框外侧并以朝向正文的内侧角
  连接外框，而不是让外框线穿过方格中心；中间正文单元格用 `3px` 内边距隔出
  第二条细框。
- 底部署名不能用普通字体字母 `T` 冒充品牌标志；项目本地化了圆形 Smartisan
  锤子 PNG。Data URL 会被公众号判定为“内容插入失败”并显示破图，因此后端会
  引用项目生产站点上的固定 HTTPS PNG；当前七牛域名只有 HTTP、HTTPS 证书无效，
  会被公众号提示“来源信息无法识别”，不能用于这个署名图标。署名改用无边框行内
  元素承载图标和文字，避免微信编辑器为展示表格画出多余底线，并可用
  `WECHAT_FOOTER_HAMMER_URL` 覆盖默认地址。PNG 圆形外部必须透明，同时在复制
  HTML 中保留 `border-radius:50%` 和透明背景；否则微信暗黑模式会把原来的暖白
  底显示成突兀方块。透明版内容哈希为
  `b5d3bd9587fa9a1226b25a0709ff61a450df29d96ca2f127c6afc0b8e193a60e`，
  已上传生产图片服务；默认地址使用这个新哈希，避开微信对旧图的缓存。
- 原版成品长图在署名下方保留了明显的纸面留白；公众号纸张容器使用宽度的 `12%`
  作为响应式底部留白，在最大 `677px` 宽度时约为 `81px`。署名本身使用固定
  `16px` 行盒和 `18px` 左缩进，避免微信二次排版破坏垂直居中和呼吸感。
- 微信公众号后台会把 `**鼓励**vibe coding` 中粗体节点与紧邻英文之间的格式
  边界当作断行点；不可断行字符不能放在 `</strong>` 之后成为独立文本节点，
  否则微信清洗富文本时可能丢弃。公众号渲染器会把不可见的 WORD JOINER 放在
  粗体或斜体节点末尾，并明确设置 `strong { display: inline }`，既跨标签阻止
  换行，也不增加可见空格。该兼容转换不能进入原始 Markdown，也不能改写围栏
  代码块和行内代码。
- 微信后台可能忽略图片单独的 `max-width: 100%` 并按固有尺寸排版；图片需要
  同时使用宽度为 100% 且隐藏溢出的实际容器、`width="100%"` HTML 属性和带
  `!important` 的内联 `width: 100%; height: auto`。
- Markdown 图片紧跟有序列表项且没有空行时会嵌套在对应 `<li>` 中；图片的
  `width:100%` 会相对带编号缩进的文字内容盒计算，导致公众号图片整体偏右。
  公众号专用预处理会把列表项后紧邻的无缩进图片拆成顶层图片，再以原编号恢复
  后续列表；围栏代码块和有意缩进的列表图片不改写。图片容器使用
  `margin:14px auto`，因此相对正文卡片居中。
- `POST /api/wechat` 会收集本地、远程、Data URL 和 HTML 图片，以 SHA-256
  内容哈希命名并上传七牛；已经位于同一七牛域名的图片直接复用。七牛对象已存在
  的 614 响应也按复用处理。
- 2026-07-31 用户明确要求公众号正文图片返回七牛 HTTP 直链
  `http://tmp-blog.fangyuanxiaozhan.com/<key>`，不能改写成外观为本站域名的
  `https://notes.fangyuanxiaozhan.com/qiniu/<key>` 代理地址。这个约束只属于
  “复制到公众号”链路；编辑器、预览、同步和内置页脚锤子图继续使用本站
  `https://notes.fangyuanxiaozhan.com/images/...`。生产环境只需把
  `QINIU_DOMAIN` 保持为上述 HTTP 域名。
- 七牛上传前使用图片二进制的 SHA-256 生成 `<hash>.<真实扩展名>` 对象键；同一
  篇文章内不同地址指向相同二进制内容时只上传一次并复用公开 URL。图片地址替换
  按源地址长度降序执行，避免查询参数地址被基础地址部分替换。
- React 19 服务端渲染会为图片自动输出顶层 preload `<link>`；这种节点不属于
  可粘贴正文，可能被公众号计为失败内容，`POST /api/wechat` 会在返回 HTML 前
  明确移除它。feedback 锁定输出中没有 preload、Data URL 或裁剪列表编号的样式。
- `npm run dev` 会先加载仓库根目录 `.env`；七牛配置再按环境变量、
  `QINIU_CONFIG_PATH`、仓库 `qiniu.json`、相邻
  `upload-local-image-to-qiniu/qiniu.json` 的顺序解析。本机真实凭据保存在被
  Git 和 Docker 构建上下文忽略的 `.env`，文件权限为 `600`；仓库只提交
  `.env.example`。Docker Compose 自动从 `.env` 注入变量，正式生产环境仍应
  使用部署平台的环境变量或 Secret。
- 七牛默认上传入口可能返回 bucket 区域提示；服务会按响应自动切换到对应
  `*.qiniup.com` 入口并在进程内缓存。
- 生产日志曾出现 `upload.qiniup.com:443` 在 TLS 建连前瞬时
  `ECONNRESET`；七牛表单上传仅在没有收到任何 HTTP 响应的网络异常时原请求最多
  重试三次，每次等待响应不超过 6 秒。公开图片仍返回既定的七牛 HTTP 域名，
  不切换代理或改写 Markdown。
- 真实图床验收上传了现有测试图片，接口返回 `imageCount = 1`、
  `uploadedImageCount = 1`，公开 URL 返回 `200 image/jpeg`；现有图床域名配置
  是 HTTP；这是当前公众号正文图片的明确产品约定，不能自行替换为本站 HTTPS
  代理。

## 2026-07-27：安卓长图图片装裱

- 安卓真机当前打开的是
  `com.smartisanos.notes.v2.share.Convert2PicturePreviewActivity`；其长图图片项
  使用 `convert_to_picture_preview_weibo_image_item.xml` 和
  `longlength_weibo_image_bg.9.png`，不是单纯给图片加普通网页边框。
- 在 `1080 × 2400`、480dpi 真机截图中，图片外层可分辨为约 `#EBE8E3` 的暖灰
  细线、`#FFFFFF` 衬边和极轻底部阴影；没有圆角和重阴影。对应布局与 9-patch
  已补充到安卓参考目录。
- `MarkdownText` 给所有正文图片增加 `note-image-frame` 标记；网页预览、PNG
  导出和离线归档统一使用 `border-box`、`max-width: 100%`、暖灰 `1px` 外线、
  按便签倍率缩放的白色衬边及轻阴影，装裱后的总宽度不会越过正文容器。
- `WechatArticle` 输出等价的内联图片框，使用 `4px` 白色衬边和
  `1px #ebe8e3` 外线，以兼容微信公众号清洗 CSS 后的粘贴和保存流程。
- 2026-07-28 再次在 `1080 × 2400` 真机长图页测量：卡片约为
  `x=35..1044`，内框约为 `x=73..1008`，图片装裱框约为 `x=133..947`；
  卡片和图片中心均约为 `x=540`，图片相对内框左右留白约为 `60/61px`。
- GFM 会把紧跟有序列表文字且前面没有空行的图片直接放进 `<li>`，使图片相对带
  编号缩进的列表内容盒居中，视觉上偏右。普通预览、PNG 导出、离线归档和公众号
  复制现在共同预处理这种无缩进图片：将其提升为正文级 `<p>`，后续列表从原编号
  继续；围栏代码和有意缩进图片不改写。
- 安卓真机 `1080px` 画布中，图片装裱框左右约为 `133 / 947px`，中心为
  `540px`，与便签卡片中心完全一致；网页浏览器验收也必须比较图片和便签内框的
  中心点，而不能只检查图片是否落在 `<li>` 边界内。

## 2026-07-28：手机列表重新以安卓客户端为唯一基准

- 用户明确要求移动版视觉和交互与已连接安卓手机中的锤子便签对齐，不再把 PC 网页版样式下沉到手机断点。
- ADB 读取设备 `b3642b48` 当前 `com.smartisan.notes/.NewNotesActivity`：物理尺寸 `1080 × 2400`、密度 `480dpi`，即 `360dp` 逻辑宽度。
- UI 层级实测顶栏 `48dp`、单条列表 `75.3333dp`、左侧装订夹 `28.6667 × 75.3333dp`、正文左边距 `52dp`、标题起点距条目顶部 `38.5dp`。
- 手机恢复 APK 的 `action_bar_default.png`、36dp 顶栏按钮、搜索框切片、木纹、九宫格纸片、装订夹、置顶、加星和图片状态图；桌面继续保留网页版 `62px / 50px` 参数。
- 装订夹仍是列表级固定轨道，不进入 sortable 条目；拖拽开始整条轨道收起 `19px`，纸片使用 APK 按下态切片且不添加包住透明区域的额外阴影。

## 2026-07-28：移动横滑删除与预览四角校准

- 安卓原版列表的删除操作通过向右滑动纸片露出左侧按钮；真机 UI 层级中纸片
  展开位移为 `66dp`，删除按钮左边距为 `22dp`。APK 的
  `btn_slide_delete_normal/pressed.png` 位于 `drawable-xxhdpi`，
  `221 × 107px` 原图按三倍密度还原为 `73.6667 × 35.6667px`。
- Web 手机列表使用 Pointer Event 做横纵方向锁定：移动不足 `6px` 时保持待定，
  横向距离明显大于纵向才接管手势；纸片最大右移 `66px`，达到 `28px` 后释放
  吸附展开。向左回滑、滚动、开始拖拽或点击其他便签会收起，同一时间最多展开
  一条；回收站不提供该入口，点击图标继续进入软删除确认。
- `360 × 800` 浏览器触摸验收确认：展开态实际矩阵位移为 `66px`，按钮实际宽度
  为 `73.664px`、左坐标为 `22px`；反向手势后矩阵恢复为零。
- `example/程序员狠话Vol.5.JPG` 的预览四角采用“方块位于外框外侧、内侧角与
  外框角相接”的几何关系。外框左右 inset 为 `8 × scale`、上下为
  `14/54 × scale`，方块边长为 `3 × scale`，因此顶部方块坐标为
  `11 × scale`、底部为 `51 × scale`；原来的 `13/53 × scale` 会让外框穿过
  方块中部。
- 移动预览 DOM 实测四角与外框连接误差小于 `0.01px`；生产构建、31 项前端
  feedback 和 2 项后端 feedback 均通过。

## 2026-07-29：账号、管理员、云同步与匿名七牛额度

- `/superadmin` 使用只存在于服务端环境变量 `SUPERADMIN` /
  `SUPERADMINPASSWORD` 的管理员凭据；管理员可创建普通用户，服务端生成 16 位
  初始密码并只返回一次。
- 普通用户密码以 scrypt 盐和哈希保存；签名会话位于 HttpOnly、
  SameSite=Lax Cookie，记住登录时有效期为 30 天，前端不保存明文密码。
- 账号、工作区和匿名额度保存在 `DATA_STORAGE_DIR/notes-data.json`。写入在单
  进程内串行，并通过同目录临时文件原子替换；Compose 已持久化挂载
  `storage/data`，多后端副本前必须迁移数据库。
- `/api/workspace` 只从签名会话取用户 ID；两个普通用户无法读取或覆盖彼此的
  工作区。前端变更延迟 650ms 保存，每 15 秒读取较新服务端版本。
- 匿名工作区继续只写浏览器 localStorage；登录时若云端为空，会用当前本地工作区
  初始化云端，云端非空则以云端为准。退出前尝试立即保存并恢复匿名工作区。
- 匿名公众号图片上传全站共享每天 500 张额度，以北京时间日期键在 0 点重置；
  对象使用 `temporary/YYYY-MM-DD` 前缀和七牛 `deleteAfterDays: 1` 策略。
  普通登录用户使用长期前缀，不执行匿名额度检查。
- feedback 已覆盖管理员登录建号、密码不落明文、普通用户隔离、500 张边界、
  北京时间重置、匿名临时上传策略和登录用户长期上传；生产构建通过，桌面/手机
  登录弹窗及管理员后台均已用本地 Chromium 验收。
- 登录弹窗的密码框提供可访问的显示/隐藏按钮；本地开发若只运行 Vite 而没有
  `3001` 后端，登录接口会失败，因此完整登录验收必须使用 `npm run dev`，或确保
  前后端两个进程同时在线。
- 普通账号标识同时接受原有的 3–32 字符用户名和最长 254 字符的邮箱；邮箱按
  NFKC 规范化并忽略大小写，管理员创建页和普通登录弹窗统一显示“用户名或邮箱”。
- 桌面账号入口替换分类栏左下角原“下载锤子便签 APP”的 `50px` 区域，未登录时
  显示“登录锤子便签”，登录后显示用户名，账号菜单在分类栏宽度内向上展开。
  `1280 × 800` 视口实测分类栏底部区域为
  `x=0..200.21, y=750..800`，登录按钮为 `x=0..200.21, y=751..800`。
- 手机端不在顶栏和分类浮窗重复显示登录入口，只在设置页“账号与同步”卡片进入；
  `390 × 844` 验收确认桌面账号区为 `display:none`，设置卡片可打开共用登录弹窗。
- 登录弹窗只提供账号密码登录，不展示未实现的短信验证码页签。普通用户可从桌面
  账号菜单或手机“账号与同步”修改密码；表单要求当前密码、8–128 字符新密码和
  二次确认。
- 普通账号持久化 `passwordVersion`，会话签发时记录同一版本。自行改密会给当前
  设备续签 Cookie；管理员重置会生成只返回一次的 16 位临时密码；两种操作都会
  递增版本，使其他旧会话失效。旧账号缺少版本字段时按版本 1 兼容读取。
- 管理员用户列表只返回 `id / username / createdAt`，没有查看现有密码的接口；
  重置按钮需要二次确认，响应和界面只短暂显示本次新生成的临时密码。
- 隔离浏览器验收确认：桌面账号菜单可改密并得到“密码修改成功”，`390 × 844`
  手机设置页显示当前账号、修改密码和退出登录；管理员列表显示“无法查看现有
  密码，只能重置”，二次确认后展示一次性新密码。完整 feedback 覆盖旧密码失效、
  旧会话失效、列表与持久化无明文密码。
- `2026-07-29` 再次使用隔离测试账号完成 PC、`390 × 844` 手机和管理员后台全
  流程浏览器验收：旧密码失败、新密码成功、管理员临时密码成功，控制台无错误。
  PC 左下账号菜单改为紧凑的无外框操作行；邮箱使用单行省略，退出项使用克制的
  警示色，避免原先两枚描边大按钮在窄分类栏中形成厚重表单卡片。

## 2026-07-30：超级管理员同时拥有便签服务能力

- `.env` 中的 `SUPERADMIN` / `SUPERADMINPASSWORD` 既可通过
  `/api/superadmin/login` 进入管理后台，也可通过便签首页使用的
  `/api/auth/login` 登录；两条入口签发同一种 `role: superadmin` 会话。
- 超级管理员是便签能力的超集：可读写以保留 ID `superadmin` 隔离存储的云端
  工作区，参与 650ms 延迟保存和 15 秒跨端轮询，并继续享受已登录用户的长期图片
  上传分支；普通用户仍不能访问任何 `/api/superadmin/*` 管理接口。
- 管理员密码继续只由服务端环境变量维护，因此便签界面不向管理员显示“修改密码”；
  这不影响其云便签、导出、归档、公众号复制和管理后台能力。

## 2026-07-30：notes-export-api 工作区管理扩展

- 仓库内 `skills/notes-export-api` 在保留原有 PNG 长图导出的同时，通过
  `/api/auth/login`、
  `GET/PUT /api/workspace` 和 `POST /api/wechat` 提供便签列表、全文读取、新增、
  文件夹分类、星标、置顶和公众号富文本命令；普通用户和超级管理员共用同一套
  Skill，数据边界仍由签名会话决定。
- 项目“分类”继续使用 `folderId` 文件夹模型，没有新增第二套标签字段。Skill
  支持按文件夹精确名称或 ID 归类，`none` 表示移出自定义文件夹。
- Skill 强烈优先使用用户本地部署的服务；`notes.fangyuanxiaozhan.com` 只在本地
  无法部署时作为公益兜底，没有 SLA，不保证持续在线、响应性能或稳定性。
- `PUT /api/workspace` 可选接收 `expectedUpdatedAt`；服务端在单进程写入队列内
  核对版本并对冲突返回 HTTP 409。Skill 写操作会重读最新工作区并最多重试 4
  次，旧网页端不传前置条件时继续保持最后写入者优先。
- 工作区 `updatedAt` 在保存时保证严格递增，可同时作为同步时间戳和条件写入
  版本。隔离 feedback 已完成“新增 → 建分类 → 归类 → 加星 → 置顶 → 公众号
  HTML”闭环，并确认过期版本写入返回 409；完整 36 项前端、8 项后端 feedback
  与生产构建均通过。
- ClawHub 已发布 `notes-export-api@0.2.0`，`latest` 标签指向该版本，许可证为
  MIT-0，共包含 6 个文件。发布后的远端核验应使用当前版 ClawHub CLI；本机旧版
  `0.7.0` 会错误报告“Version not found”，而 `0.23.1` 可正常读取版本、文件
  清单和变更日志。
- ClawHub 的版本扫描把“本地不可用时自动回退公益服”标为需要人工复核，因为该
  路径可能把便签正文、凭据或本地图片发送给公网服务；这不是恶意软件拦截，但后续
  版本应考虑把公网回退改成显式授权，进一步强化本地部署优先和数据边界提示。

## 2026-07-30：移动编辑光标和详情状态栏坐标

- 早期 `22px` 镜像自绘光标虽然能在普通点击时对齐文字，但无法可靠复刻 iOS
  空格键触控板的原生 selection 命中，已在 2026-07-31 撤销。移动端现在只使用
  textarea 系统光标，并让其行框与 `42px` 横线周期共用同一行高。
- 详情状态栏必须延续正文纸张的左侧深色纸轨。两段共用
  `--mobile-editor-gutter-width: 25px`、相同纹理和右侧分隔线，文件夹控件位于
  纸轨右侧 `10px`，其左坐标为 `35px`；不能为了修复上下宽度不一致而删除状态栏
  纸轨。正文仍保持安卓版 `39px / 18px` 左右留白。
- 桌面横线图片 `grid_6e4a41eefc.png` 的固有高度为 `50px`，不能用于行高
  `42px` 的移动编辑器。移动端改用以 `--editor-line-height` 为周期的 CSS
  渐变横线，线位于行框最后 `1px`，并使用 `background-attachment: local`
  跟随 textarea 滚动；这样文字与横线不会每行累积 `8px` 偏移。左侧纸轨的
  `edge_004e88bdf2.png` 也有 `50px` 固有高度，移动端必须缩放为
  `5px × var(--editor-line-height)`，并把背景纵坐标同步为 textarea 的负
  `scrollTop`，这样纸轨格线、正文横线和文字在静止及滚动后都保持同相位。
- 2026-07-30 ADB 真机复核：OnePlus 8 为 `1080 × 2400 / 480dpi`，锤子便签
  `4.2.1` 详情页横线物理坐标为 `523–524、649–650、775–776……`，周期严格为
  `126px = 42dp`；左纸轨在相同纵坐标绘线，主体宽 `75px = 25dp`，右侧另有
  `1px` 物理分隔。原生正文横线为 `#f4ebde`、厚 `2` 个物理像素，即
  `0.6667dp`；网页旧值 `#e5ded4 / 1px` 反差过强，会在相同 `12dp` 字线距离下
  造成更拥挤的观感。以后移动编辑纸测试必须覆盖正文周期、纸轨周期、两者相位、
  线色/线厚和非整周期滚动后的相位，不能只断言行高变量。

## 2026-07-30：官方短便签导出几何基线

- 使用相同正文“Mac mini 是一个很神奇的小工具，大家认为它可以成为一种算力的
  新标准”对比官方导出和项目真实 `/api/export` 成品。把官方图等比归一到
  `1980px` 宽后为 `1980 × 1144`；校准后的项目成品为 `1980 × 1146`，高度只差
  `2px`。
- 以灰度阈值 `72%` 测量两行正文包围盒：官方为
  `x=194, y=337, w=1561, h=183`，项目为
  `x=195, y=337, w=1547, h=183`；首行都结束于“它”，第二行都从“可以”开始。
  以后调整字体或正文留白时必须保持这一短便签换行基线。
- 暖白成品纸使用 `#fffcf7`，双框使用 `#e8e4dc`，正文使用 `#665749`。
  正文为 `0.76rem × --note-scale`、`1.8` 行高和 `0.03em` 字距；正文区顶部留白、
  双框 inset、四角位置以及页脚字号/间距均已按官方图片独立校准，不能再用整体
  缩放替代。
- OPPOSans Regular 的原始笔画比官方参考略细，但直接切到 Medium 会改变字宽并
  破坏已校准的换行。正文继续保持 `font-weight: 400`，通过
  `calc(0.15px × --note-scale)`、`62%` 当前文字色的半透明
  `-webkit-text-stroke` 轻微增重；真实导出仍为 `1980 × 1146`，换行和正文坐标
  不变，中间灰度阈值下的笔画覆盖面积与官方误差约 `3–4%`。
- 网页预览与 Playwright PNG 复用 `src/styles.css`；离线归档的内嵌样式位于
  `server/index.ts`，公众号富文本颜色位于 `WechatArticle.tsx`。三条链路需要
  同步更新并由 feedback 测试锁定，避免纸色、框色或文字色漂移。
- 本轮校准完成后，37 项前端 feedback、8 项后端 feedback 和生产构建通过；
  局域网页的真实 `NoteSheet` DOM 复核无控制台错误。

## 2026-07-30：移动编辑器文本选区基线

- 官方参考图取色：普通纸面约 `#faf7ee`，选区约 `#e8e0d5`，端点手柄约
  `#a68b75`。在纸面上叠加 `rgba(166,139,117,.20)` 可复现该选区颜色。
- `.markdown-editor::selection` 和 Firefox 的 `::-moz-selection` 统一使用
  `--editor-selection-bg`；所有平台的系统原生光标使用
  `--editor-caret-color`。暗黑主题使用独立的暖金色变量，不复用亮色纸张值。
- 曾实现过镜像文字、自绘光标和自绘选区端点，但 iOS 空格键触控板会让真实
  selection 与覆盖层不同步，出现两个同时闪烁的光标；该方案已撤销，不能恢复。
- 选区首尾、拖动命中和剪切/复制菜单全部由系统处理，网页只保留选区背景颜色。

## 2026-07-30：安卓官方暗黑主题基线

- ADB 真机为 OnePlus 8（`1080 × 2400`、`480dpi`），锤子便签 `4.2.1`。列表页
  和编辑页截图分别保存到临时文件
  `/tmp/codex-smartisan-dark-list.png`、`/tmp/codex-smartisan-dark-editor.png`。
- 官方暗黑模式不是偏蓝黑渐变。像素与 APK 资源基线为：列表底纹 `#131113`、
  卡片 `#1e1c1e`、搜索框 `#201e20`、次级文字 `#676467`、主文字 `#cecece`、
  编辑纸面 `#1c1a1c`、左纸轨 `#161416`、详情状态栏 `#1a181a`、横线和分隔线
  `#282828`。
- 已从应用内置 `assets/dark_skin.zip` 复用官方暗黑顶栏、背景、搜索框、列表卡片、
  装订夹、星标、图钉、图片标记及横滑删除素材，项目路径为
  `public/smartisan/mobile/dark/`。Android `.9.png` 列表和搜索素材按原始
  `3×` 边界切成左、中、右三段，保留固定装订线位置。
- `390 × 844` 局域网页实测列表、编辑和设置三页：列表背景、正文、编辑纸面、
  纸轨、横线和状态栏计算样式均与上述基线一致，控制台无错误。暗黑设置页也统一
  使用中性暖黑底纹和卡片，避免切换页面后回到刺眼的浅色界面。

## 2026-07-30～31：iOS 可视视口与触控命中基线

- iOS Safari / Chrome 展开顶部地址栏、从页面缓存恢复或显示软键盘时，
  `visualViewport.height` 会变化；应用壳继续同步这个高度，使列表、编辑器和
  预览区只占用键盘上方的实际可见空间。
- 不得用 `visualViewport.offsetTop` 平移固定定位的 `.app-layout`，也不得监听
  `visualViewport.scroll` 后强制 `window.scrollTo(0, 0)`。iOS 地址栏动画会让
  纵向原点短暂变化，绘制层被移动后，浏览器原生触控高亮和实际命中区域可能仍按
  原坐标计算，表现为标签需要偏着点、点击第一条却命中第二条。应用壳固定
  `top: 0; left: 0; right: 0`，仅在 resize、pageshow 和横竖屏切换时同步高度。
- iOS 26 Safari 真机录屏中，应用壳右边界从 `1320px` 移到 `1232px`，按设备
  `3×` 比例恰好对应 `440px → 410.7px`，并在右侧露出约 `29.3px` 空带。旧实现
  唯一的横向动态输入正是 `visualViewport.width`，注入同一异常值可稳定复现；
  移除后浏览器几何回归保持 `left/right/width = 0/440/440px`。水平方向必须固定
  `left: 0; right: 0; width: auto`，只用 safe-area 环境变量留出刘海边缘；
  不得再同步 `visualViewport.offsetLeft / width`。
- `500 × 1061` 浏览器移动视口实测：列表页应用壳与 Header 为
  `0,0 → 500,1061` / `0,0 → 500,48`，进入编辑页后 Header、`50px` 状态栏和
  编辑器依次位于 `0–48 / 48–98 / 98–1061px`，`window.scrollX/Y` 均为 `0`，
  左右边界始终为 `0 / 500px`。

## 2026-07-30：桌面暗黑主题基线

- PC 端复用安卓官方暗黑色板，但不复用移动端的卡片布局：分类栏、便签栏、正文
  工具栏和面板统一为 `#1e1c1e`，编辑纸面 / 实时预览为 `#1c1a1c`，左纸轨为
  `#161416`，横线与分栏线为 `#282828`。
- 桌面正文和列表主标题使用 `#cecece`，时间、字数和次级状态使用 `#676467`；
  Markdown / 实时预览菜单、搜索框、账号入口与底栏必须一起暗化，不能只改外壳。
- 暗黑桌面顶栏的刷新、新建、插图、转移、AI、分享和删除七个按钮使用完全相同的
  `#3a363a → #2b282b` 渐变、`#4a464a` 边框和阴影；删除按钮在暗黑模式不保留
  红底。悬浮与分享展开态统一为 `#454045 → #343034` 和 `#5c575c` 边框。
- 暗黑适配只覆盖 `641px` 以上 PC 工作区的颜色与纹理，继续遵守桌面网页版三栏
  比例、`50px` 编辑行距、`202 × 110px` 模式菜单和既有字体渲染基线。
- `1536 × 900` 局域网页实测编辑态和实时预览态：三栏表面、纸面、纸轨、横线、
  主次文字及模式菜单计算色值全部命中上述基线，浏览器控制台无 warning/error；
  37 项前端 feedback、8 项后端 feedback 和生产构建通过。
- 桌面编辑器滚动时，`.markdown-editor-frame::before` 的背景纵坐标必须复用
  `--editor-paper-scroll-y`，该变量由 textarea 的 `-scrollTop` 驱动。暖白主题
  在 `0 / 1 / 49 / 50 / 51 / 137 / 499px` 七个滚动位置逐像素取样，`25px`
  左纸轨与正文横线行号完全一致。暗黑主题修复后又在
  `971 / 1812 / 2773 / 3519px` 四个滚动位置复核：正文背景均为
  `local, local`，纸轨偏移始终严格等于负 `scrollTop`。
- 正文横线属于 textarea 背景，必须使用 `background-attachment: local` 跟随
  文字内容滚动。暗黑主题曾用 `background` 简写覆盖纸面并把它重置为默认
  `scroll`，造成正文横线固定、文字和纸轨继续滚动；移动与桌面的暗黑覆盖规则
  都必须在简写之后重新声明 `local`，feedback 会检查两个断点。
- PC 分类栏底栏采用“账号自适应宽度 + `50px` 设置入口”的两列结构。桌面与移动
  端共用唯一的 `SettingsPanel` 实例，桌面弹层固定在左下角向上展开，移动端仍
  保持全屏设置页。`1536 × 900` 实测按钮为 `49 × 49px`、弹层为
  `340 × 274px @ (10, 568)`；暖白 / 暗黑主题切换、外部点击关闭以及
  `390 × 844` 移动设置回归均通过，控制台无 warning/error。

## 2026-07-30：便签底部显示设置基线

- OnePlus 8 真机上的锤子便签 `4.2.1` 设置一级页使用“Footer Display”入口，
  二级页以两张大型便签预览卡选择“由锤子便签发送 / via Smartisan Notes”或
  “由某便签发送 / via Some Notes”。Web 版保留“入口 → 二级页 → 便签预览”的
  层级，但把预设选择扩展为用户可编辑的两段文字。
- 设置二级页包含实时缩略预览、发送来源输入框、`via` 输入框、各自的
  `80` 字符计数和“恢复默认页脚”按钮；清空其中一项会隐藏对应署名段落。
  缩略图和成品一致保留双框与四个小方格，设置缩略图的小格为 `6 × 6px`，中心
  必须落在外框交点。暖白和锤子暗黑主题都使用各自的纸面、边框和输入框 Token。
- Logo 支持上传 PNG、JPG、WebP 或 GIF，使用既有 `/api/images/import` 导入并
  只持久化 `/images/*` 相对地址；可单独恢复默认锤子 PNG。浏览器持久化键为
  `notes.footerLogoUrl`，默认值为
  `/smartisan/web/smartisan_hammer_footer.png`。
- 文本持久化键为 `notes.footerBrand` 和 `notes.footerVia`；URL 中
  `footerBrand` / `footerLogoUrl` / `footerVia` 查询参数优先。预览内就地编辑与
  设置页共享同一状态，PNG 导出、离线归档和公众号复制也全部使用这组值；
  `POST /api/wechat` 接受同名字段，服务端渲染不再写死默认署名或 Logo。
- 离线归档会把当前 Logo 写入 `html.assets/footer-logo.*` 并把 HTML 改为相对
  路径；公众号会把自定义 Logo 与正文图片一起按内容哈希去重后上传为 HTTPS。
- 桌面暗黑设置页实测缩略图为 `310 × 148px`，外框相对边距为
  `17 / 21 / 17 / 51px`；四个小格均为 `6 × 6px`，中心坐标与外框四个交点严格
  相同。弹层 `clientHeight` 与 `scrollHeight` 均为 `600px`，没有裁切。
- 内置暖白锤子 Logo 在暗黑主题下使用
  `grayscale(1) brightness(0.56) contrast(2.4)` 与 `opacity: 0.72`，使圆标
  与页脚署名处于相近的低对比层级；设置缩略图和成品预览一致。滤镜仅挂在
  `is-default-footer-logo`，用户上传的自定义 Logo 保持原色。默认 PNG 必须为
  48 × 48 RGBA，四角 Alpha 均为 `0`；旧素材只有左上角透明，会在暗黑纸面上
  暴露出三角形/方形亮块。
- 使用真实 `/api/images/import` 上传默认锤子 PNG 后返回内容寻址的
  `/images/b5d3bd…a60e.png`；设置缩略图和真实 `NoteSheet` 同时读取该地址，
  “恢复默认”会把两处立即切回内置资源。真实 `/api/archive` ZIP 已确认包含
  `html.assets/footer-logo.png`，`index.html` 引用相对路径。
- `1536 × 900` 桌面弹层与 `390 × 844` 手机全屏页均完成浏览器回归：编辑后
  缩略预览立即更新，刷新后持久化，真实 `NoteSheet` 页脚同步；暗黑主题无溢出，
  恢复默认后输入框、预览和 localStorage 同时复位，控制台无 warning/error。
- 本轮 39 项前端 feedback、8 项后端 feedback 和生产构建通过。

## 维护者验收偏好

- 每次完成一项功能修改并通过本地验证后，在交付信息中显示当前可用的局域网访问
  地址（`http://局域网 IP:端口`），方便维护者从手机或其他局域网设备复测。
- 只显示地址还不够：验收服务必须监听局域网可达的接口（通常为 `0.0.0.0`），
  不能继续使用仅本机可访问的 `127.0.0.1` 监听地址；同时保留本机地址便于排障。

## 2026-07-31：手机木纹单图铺满基线

- ADB 读取 OnePlus 8 上锤子便签 `4.2.1` 的列表截图与 UI 层级，并与 APK 的
  `activity_notes.xml` 交叉确认：`mainRootLayout` 直接以
  `@drawable/note_background` 作为根背景，列表控件没有自己的木纹背景。
- `note_background.png` 的 xxhdpi 原图为 `480 × 819px`。Android
  `BitmapDrawable` 在该根布局中把单张素材填满可视区域，而不是按
  `160 × 273dp` 固有尺寸重复；真机整屏只出现一幅连续木纹。
- Web 手机断点改为由 `.app-layout` 使用
  `background-repeat: no-repeat; background-size: 100% 100%` 承载单张背景，
  `.note-sidebar` 与 `.note-list` 透明。暖白、锤子暗黑和手机预览舞台都使用
  同一单图铺满规则，避免父子层各自平铺造成横纵接缝。
- `360 × 800` 浏览器实测暖白与暗黑列表：根背景计算样式均为
  `no-repeat / 100% 100%`，侧栏和列表的背景图均为 `none`；预览舞台同样为
  单图铺满，卡片、搜索框和便签纸布局无回归。随后在同一台
  `1080 × 2400` 安卓手机的 Chrome 中通过局域网再次截图确认，暖白木纹从左到右
  及从搜索框下方到底部都保持连续，没有横纵拼接接缝；前端 46 项 feedback 与
  静态生产构建全部通过。

## 2026-07-31：手机 Markdown 快捷输入栏基线

- 在 OnePlus 8（`1080 × 2400`、480dpi）的锤子便签 `4.2.1` 中新建临时便签
  逐项实测：快捷栏只在软键盘显示时存在，物理边界高 `128px`，折算为
  `42.6667dp`；五个按钮各宽 `216px = 72dp`，依次为 `# Title`、
  `[Center]`、`- List`、`**Bold**`、`> Quote`。
- 标题按钮按一级、二级、三级标题循环；居中按钮用 `[]` 包裹当前行，空行光标
  位于括号内；列表和引用行回车会自动续写前缀，在仅有前缀的空项目上回车会退出；
  已有列表或引用前缀不会重复添加。
- 原版加粗按钮在没有选区时不改写内容，但 Web 工具栏作为 Markdown 符号助手，
  按当前产品要求在折叠插入点添加一个 `**` 并把光标放在标记后；有选区时仍用
  前后两组 `**` 包裹并继续选中内部原文，所以重复点击会继续嵌套星号。
- Web 实现位于 `src/lib/markdown-shortcuts.ts` 和
  `src/components/EditorPanel.tsx`。按钮 `pointerdown` 阶段阻止默认聚焦，
  以保留 textarea 选区、输入焦点和软键盘；`visualViewport` 负责在键盘收起时
  隐藏工具栏。
- 同一台手机的安卓 Chrome 真机验收中，网页工具栏边界为
  `[0,1307][1080,1433]`，同样是 `128px` 高和五等分；点按标题、列表、居中、
  引用后键盘与编辑焦点均保持，列表续行/空项退出生效，收起键盘后工具栏从
  UI 层级消失。
- 快捷操作纯函数与移动端结构新增 6 组 feedback；最终前端 52 项、后端 9 项
  全部通过，应用与服务端类型检查及 Vite 生产构建通过。

## 2026-07-31：iOS 便签正文自动填充语义

- iOS Chrome 的钥匙、银行卡、地址和上一项/下一项按钮属于 Chrome 自己的
  `FormInputAccessoryView`，不是项目的 Markdown 快捷栏，也不在网页 DOM 中。
  Chromium 内部以 `passwordButtonHidden`、`creditCardButtonHidden` 和
  `addressButtonHidden` 控制这些按钮；网页 CSS/JavaScript 没有对应控制接口。
- `autocomplete="off"` 只是正确的字段语义和最佳努力提示，iOS 浏览器仍可能忽略。
  用户如需隐藏银行卡入口，需在 Chrome“设置 → 付款方式”关闭“保存并填充付款方式”；
  项目不能承诺从网页侧隐藏原生附件。
- 曾尝试在 iPhone / iPad 将正文替换为 `contenteditable="plaintext-only"`，真机
  Chrome 证明该方案既没有消除手动填充入口，又造成正文与 `42px` 横线错位、纸轨
  相位脱节、光标定位失效，并额外出现上一项/下一项输入附件。该实验已回退。
- 所有平台统一保留 textarea，继续复用已验证的 Markdown 字符串、选区索引、回车
  续行、粘贴、插图、快捷栏、系统光标和负 `scrollTop` 纸轨同步。不要再以替换
  编辑控件的方式对抗 iOS Chrome 原生界面。
- 登录与修改密码弹窗继续保留 `username`、`current-password` 和 `new-password`
  的标准语义，正文修复不能以牺牲正常密码管理器体验为代价。
- 回归测试锁定正文 textarea 的普通文本语义以及纸轨从
  `textarea.scrollTop` 获取相位，防止重新引入 `contenteditable` 路径。

## 2026-07-30：AI 逐条审阅基线

- `.env` 中的 `OPENAI_API_KEY`、`OPENAI_BASE_URL` 和可选 `OPENAI_MODEL` 只进入
  Express；Compose 需要显式透传，`.env.example` 的密钥必须为空，任何变量都不
  得使用 `VITE_` 前缀。启动时 `/models` 探测失败不阻断便签服务。
- `GET /api/ai/status` 只公开 `{available}`；`POST /api/ai/suggestions` 必须使用
  普通用户或 superadmin 会话，并包含同源校验、`128kb` 独立 JSON 解析限制、
  单账号并发锁和 `10 次 / 10 分钟` 频率限制。
- DeepSeek 2026-07-24 后默认模型为 `deepseek-v4-flash`，请求使用 JSON Output
  且关闭 thinking。模型不得返回整篇改写稿；后端只接收可在源 Markdown 中唯一
  精确定位、互不重叠的 `original / replacement / reason` 原子建议。
- 设置页只有在启动探测成功后才显示 `notes.aiEnabled` 开关。AI 入口读取当前
  便签快照，返回结果时正文保持不变；每条建议分别提供“确认修改”和“忽略”，
  没有自动修改或全部接受。确认时从源快照和已确认 ID 倒序重建 Markdown；若当前
  便签或正文已变化，拒绝应用旧建议。
- 使用真实 DeepSeek 配置完成浏览器验收：返回 8 条独立建议，未点击确认前正文
  DOM 快照逐字一致。46 项前端、9 项后端 feedback 和生产构建通过。
- AI 审阅弹层的“生成修改建议”和逐条“确认修改”必须复用
  `--export-button-*` 主题按钮 Token：暖白主题为暖棕，暗黑主题为中性灰黑。
  黄绿色仅适合作为小面积状态提示，不能用于弹层中的大面积主按钮。
- iOS Chrome 在正文聚焦时触摸顶栏，会先触发 textarea 失焦、软键盘收起和
  `visualViewport` 重排；如果 AI 只监听 `click`，按钮可能在 touchstart 与 click
  之间移动，导致 WebKit 取消第一次点击。AI 按钮对 touch pointer 在
  `pointerdown` 阶段打开，鼠标和键盘仍使用标准 `click`。
- AI 打开状态使用单一的 `aiReviewNoteId`，不再混用布尔 state 与可变 ref。
  审阅层由 `createPortal(..., document.body)` 脱离移动端
  `.app-layout { position: fixed; overflow: hidden; }`，避免 iOS WebKit 在键盘
  视口切换时裁掉固定弹层。
- 本轮移动端 AI 弹层与纸张横线相位修复完成后，58 项前端 feedback、9 项后端
  feedback、完整 TypeScript 检查和生产构建均通过；移动端浏览器实测弹层父节点为
  `BODY`，边界为完整 `440 × 956` 视口。

## 2026-07-31：移动端固定一倍视口

- 手机网页按应用式固定画布处理，不允许双指缩放。`index.html` 的 viewport 同时
  锁定 `initial/minimum/maximum-scale=1` 并声明 `user-scalable=no`。
- iOS Safari 与 iOS Chrome 共用 WebKit，可能不执行 viewport 的缩放上限；手机
  断点因此还在 `html / body / #root` 使用 `touch-action: pan-x pan-y`。该值保留
  横纵拖动而排除 `pinch-zoom`；不可替换成仍包含缩放能力的 `manipulation`。
- iPhone 会在聚焦小于 `16px` 的表单控件时自动放大页面。移动端所有文本类 input、
  textarea 和 select 统一至少为 `16px`，正文原有 `16px/42px` 节奏不变。

## 2026-07-31：手机空白新便签返回语义

- 手机点击“新建便签”后会单独记录本次草稿 ID；若正文仍只有空白字符，返回列表
  时直接丢弃该草稿，并原子切换到同分类的真实下一条，避免列表残留“新便签”以及
  可见行与实际打开 ID 错位。
- 只清理当前新建流程产生的草稿。已有便签即使为空也不会因普通返回被误删；若
  工作区没有其他正常便签，则保留唯一空白便签，维持应用始终有可编辑入口。

## 2026-07-31：iOS 空格键触控板与自绘光标

- iOS 长按空格进入键盘触控板时直接控制 textarea 的真实 selection；镜像文字定位
  即使监听 `selectionchange`，仍可能因 WebKit 原生光标临时显现、动画帧时序和
  中文/Markdown 混排测量产生双光标或命中偏差。
- 最终方案是彻底删除镜像层、自绘光标、自绘选区端点和闪烁动画，只保留系统
  textarea 光标。系统光标高度不能用 CSS 独立设置，移动端通过统一
  `--editor-line-height: 42px` 让 WebKit 光标行框与两条横线的周期一致。
- `document.selectionchange` 只负责保存最新 `selectionStart / selectionEnd`，
  供 Markdown 快捷栏和插图操作读取，不再参与任何视觉绘制。
- iPhone 真机 `1320px` 截图中正文横线物理纵坐标为
  `791/792、917/918、1043/1044……`，周期 `126px = 42 CSS px`。曾为了匹配特定
  WebKit 光标边界把横线相位设为 `-10px`，但这会把首个可见行框缩成 `32px`，并
  让原生文字相对横线整体偏下。
- 最终以原生 textarea 行框为唯一排版基准：`--editor-rule-offset-y: 0px`，首行与
  后续行都保持完整 `42px`。系统光标、选区与插入点由 iPhone/WebKit 自行绘制，
  不再移动纸张、文字或 textarea 追逐系统光标边界。正文渐变横线与左纸轨仍分别
  使用 `0 var(--editor-rule-offset-y)` 和
  `calc(-scrollTop + var(--editor-rule-offset-y))`，保证滚动后的共同相位。

## 2026-07-31：公众号同源图片与锤子式正文内图片编辑

- 旧便签和自定义页脚 Logo 可能保存完整的生产站点
  `https://notes.../images/...` 地址。`POST /api/wechat` 不能用 Node `fetch`
  绕公网抓取自己的持久化图片；生产容器的 DNS、证书或回环路径不可用时只会返回
  `fetch failed`。
- `prepareWechatArticle` 接收由请求协议与 Host 计算的 `publicBaseUrl`。图片 URL
  与它 Host 相同且路径以 `/images/` 开头时，`resolveArchiveImage` 直接从
  `IMAGE_STORAGE_DIR` 读取；外站 HTTP(S) 图片才走下载。feedback 使用不可解析的
  `notes.example.invalid` 同源 URL，确保测试不会因本机网络恰好可用而假通过。
- 安卓真机锤子便签 `4.2.1` 实测：编辑态图片是同一文档滚动流中的独立块，文字
  可以分别出现在图片前后；图片按自然比例接近铺满纸面，并有白色装裱、轻微阴影，
  右上角突出一个约 `32dp` 的白色三横线圆形操作柄。它不是键盘上方的固定缩略图条。
- Web 编辑器继续只使用系统原生 textarea 光标，但按图片起止索引把 Markdown
  拆为交替的文本段和图片块；图片源码不丢失，保存和同步仍得到原始 Markdown。
  点击三横线操作柄会聚焦图片后的文本段；在相邻文本边界按 Backspace/Delete
  可删除图片标记，不需要切换到 `contenteditable` 或绘制第二个光标。
- 手机 CSS 几何按真机换算为：图片外框左侧 `32px`、右侧 `25px`，操作柄
  `32 × 32px`、`top: 14px`、`right: -13px`。`390 × 844` 浏览器实测单图外框
  `333px` 宽，页面横向溢出为 `0`。
- 文本段按 `42px` 行高自动扩展。图片自然高度不是 `42px` 整数倍时，运行时只在
  图片底部补齐网格余量；实测图片后文本段相对滚动流起点为 `1680px = 40 × 42px`，
  后续系统光标与纸面横线不会因图片累计错相。
- 最终 60 项前端 feedback、9 项后端 feedback、完整 TypeScript 检查与生产构建
  通过；构建只保留既有的单 chunk 超过 `500kB` 提示。

## 2026-08-01：移动端后续行 caret 高度实机复现与修正

- 之前把问题归因为“空行原生 caret”是错误诊断。iPhone 15 Pro / iOS 17 Safari
  模拟器用 `第一行文字 / 第二行文字 / 第三行文字` 复现确认：第一行插入点约为
  `22 CSS px`，第二、第三及后续行即使有文字，也会被 WebKit 画成接近完整
  `42px` 行框。`U+200B` 空行占位对这个问题无效，已连同显示/源码索引映射全部删除。
- 所有正文仍使用原生 textarea，原始 value 就是 Markdown。移动断点只在 textarea
  聚焦、selection 折叠且不处于 composition 时，将系统 caret 临时设为透明，并用
  不可见测量层定位唯一的 `22px` 固定插入点。桌面端继续完全使用系统 caret。
- 固定插入点不接收事件、不绘制文字或选区端点；selection 展开、pointerdown、失焦
  或 IME composition 时立即隐藏并恢复系统 caret，因此选区背景、系统手柄、拖动、
  复制、剪切和键盘输入仍由浏览器原生 textarea 负责。这与 7 月 31 日曾被移除的
  “自绘 caret + 自绘选区端点”方案不同，后者的问题部分不会恢复。
- 修复后同一台 iPhone 模拟器逐行截图确认三行均为短插入点。Pixel 10 Pro XL /
  Android Chrome 通过 CDP 读取三行几何：`top = 109 / 151 / 193px`，相邻行严格
  相差 `42px`，三行 `height` 均严格为 `22px`；第二行模拟器截图也确认短插入点与
  文字等高。
## 2026-08-01：PC 长便签滚动与二级标题回归

- 分段 textarea 的自动高度计算曾在每次输入时先把高度
  设为 `0`，导致滚动容器内容瞬时收缩并把 `scrollTop` 从下方位置钳制到顶部。
  高度调整现在必须保存并恢复外层 `.markdown-editor-flow.scrollTop`，同时关闭滚动
  锚定；本地 `1536 × 900` 实测下方输入前后均保持 `331px`，焦点继续留在原 textarea。
- 左纸轨由 `.markdown-editor-frame::before` 绘制，因此 `--editor-paper-scroll-y` 必须
  写在 frame 祖先本身，不能只写到后代 flow。修复后在 `scrollTop = 662px` 时 frame、
  flow 和伪元素背景纵坐标都为 `-662px`，左右 `50px` 横线保持同相位。
- `splitSections` 会移除 `##` 并把后续内容存入 `NoteSection.heading`。`NoteSheet` 必须
  把非居中 section heading 显式渲染为 `h2`，并只按行内 Markdown 解析；数字开头的
  `## 1. 标题` 不得降级为 `<ol>`。当前 H2 为 `0.92rem × note-scale / 700`，正文为
  `0.76rem × note-scale / 400`；桌面实测字号分别为 `29.44px` 和 `24.32px`。
